import $ from './query';
import Entry, { isNode, isNodeEntry, isNodeList } from './entry';
import Event from './event';

export default $;

export { Entry as NodeEntry, Event, isNode, isNodeEntry, isNodeList };
