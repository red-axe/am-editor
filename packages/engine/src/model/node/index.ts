import $ from './query';
import NodeEntry, { isNode, isNodeEntry, isNodeList } from './entry';
import Event from './event';

export { NodeEntry, Event, isNode, isNodeEntry, isNodeList };

export default $;
